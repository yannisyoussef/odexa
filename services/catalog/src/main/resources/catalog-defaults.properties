# Imported after runtime defaults so service defaults win Spring ConfigData precedence.
server.port=${SERVER_PORT:8081}
runtime.events.enabled=false
spring.sql.init.schema-locations=classpath:runtime-schema.sql,classpath:schema.sql
spring.sql.init.data-locations=optional:classpath:data.sql
